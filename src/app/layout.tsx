import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import type { ReactNode } from "react";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Free Character Profile & Story Bible Starter",
  description:
    "Build structured character profiles, a relationship list and a world starter, then export the whole Story Bible to hand to a collaborator. Free, no account required.",
};

// Typed explicitly rather than with the generated `LayoutProps<"/">`, so
// `npm run typecheck` works on a fresh clone before the first build.
export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="flex min-h-full flex-col">{children}</body>
    </html>
  );
}
